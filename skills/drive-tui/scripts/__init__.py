"""Installable SmartCLI drive/MCP command package.

The files in this directory remain directly executable so the drive-tui skill
is still self-contained when copied outside the repository. Packaging maps the
same directory to :mod:`smartcli_drive`; no second implementation is maintained.
"""
