"""Recipe modules (one interaction paradigm each); auto-imported by
``patterns.registry.load_all`` via pkgutil. Drop a new ``*.py`` with an
``@register``-ed Pattern subclass here and it joins classify() automatically.
Names starting with ``_`` are skipped."""
