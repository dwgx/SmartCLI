"""Drop-in widget extensions.

Any ``.py`` module here that calls ``@ui.registry.register`` on a Widget subclass
is auto-discovered by ``registry.load_all()``. See the 'add a widget' recipe in
SKILL.md.
"""
