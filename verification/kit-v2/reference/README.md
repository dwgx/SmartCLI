# 历史原包，不是 v2 的执行顺序
`v1_original.zip` 与本次可访问的旧计划 ZIP 字节相同，未改其 49 项清单。`earlier_audit_original.zip` 保留旧审计、旧评分工作簿与修正目录，解决本地未下载资料的缺口。

这些是历史快照：旧数字不是新测量，旧任务调度不覆盖 v2。不要运行旧 `agentctl next` 就跳过 `docs/ORDER.md`；本轮不自动迁移或覆盖你已经存在的 state.json。旧包解压后的使用仍受当前本机权限限制。

v2 七份增量位于顶层 `docs/`，原计划仍原样封存。需要放回旧计划目录时，建立 `docs/v2/` 和独立 `v2-tools/`；不要覆盖旧 MANIFEST、入口或缓存前缀。最简单的是整包独立使用。

## 原评分表位置（旧评估，不是新跑分）
- `earlier_audit_original.zip` 内：`SmartCLI_Handoff_2026-09-15/SmartCLI_SCORECARD.xlsx`
