YOU ARE THE WORKER. DO NOT SPAWN.
Do not dispatch Claude, Codex, or Grok. Do not call a dispatch skill.
Do the job in this session. Write the result to the approved path below.

Cwd: D:\Project\SmartCLI (resolve and verify it, do not guess another checkout).
Kit: D:\Project\SmartCLI-v2-handoff (or the exact supplied extraction path).
Python: D:\Software\Developer\Python314\python.exe (use the verified interpreter).
Run: the owner's approved D:\Project\SmartCLI-v2-runs directory, outside repo/kit.
Read current repository instructions, docs/ORDER.md and this brief. Do not modify
harness configs, cached prefixes, global dependencies, or anything outside the
approved project/run roots. Do not commit, push, publish, start Docker, or run
heavy/real-PTY tests without explicit separate authorization. Respect existing
uncommitted work and the single-writer rule. Use the existing CodeGraph as a
best-effort navigator; follow abstract backend calls and vendor twins manually.

Source comments and test names: English. Report to the owner: Chinese.

# T05 — the selected label must be 保存, not 3

## Goal and completion
The supplied Chinese menu already reproduces on the real core (local E3). Fix
cell-column label extraction, not the selection heuristic or terminal engine.
Produce an original-red / candidate-green / bug-restored-red / restored-green
closure with unchanged literal test oracles. Do not re-run the original seed sweep.

## Exact scope: at most THREE files
- smartcli_core/snapshot.py :: build_snapshot (a private helper here is optional).
- skills/drive-tui/_vendor/smartcli_core/snapshot.py :: byte-identical generated twin.
- tests/test_cell_span_regression.py :: new regression; copy/adapt the kit template.

Do NOT edit screen_model.py, session.py, tui.py, mcp_server.py, pyproject.toml,
registries, workflow/config files, or unrelated tests. If this scope is insufficient,
stop and give a minimal counterexample and scope request. No line-count target.

## Preserve
Existing span half-open coordinates, label .strip() behavior, body text, JSON fields,
ASCII results, widest-highlight heuristic, cursor/status hints. Do not claim this fixes
all grapheme width issues. INTERLEAVE_OK/on_poll/resize deferral remain untouched.

## Implementation freedom
Aggregate glyph data from cells in [start,end); empty continuation cells contribute
nothing. Include only bases whose cell lies in range; do not fetch a preceding base
outside the range. Inline is fine; a private helper is fine. Expected labels must be
literal fixture values rather than computed with your helper.

## Vendor safety
The official sync_vendor.py refresh deletes the entire vendor tree before copying it.
Do not call refresh blindly. Confirm the twin had no independent edit, then copy only
the allowed canonical file to the exact existing twin using your file tool. Do not
recreate the directory or bypass hooks. Read-only --check and test_vendor_sync remain
required. If other vendor files already differ, report the pre-existing drift and stop
before broad synchronization.

## Commands (PowerShell variables resolved to exact approved paths)
& $Python -B tests/test_cell_span_regression.py
& $Python -B tools/sync_vendor.py --check
& $Python -B tests/test_vendor_sync.py
& $Python -B tests/test_terminal_fidelity.py
& $Python -B "$Kit/tools/conformance.py" --repo $Repo --case screen --consent-execute-reviewed-code

The first command must fail for the intended business assertion against the baseline.
For mutation, keep the new test unchanged and restore only the old display[y][a:b]
logic in an isolated approved validation copy. Never reset/revert the user's live tree.
Restore the candidate and re-run its affected gates. Keep first-run output.

## Stop conditions
Concurrent writer, unaccounted dirty files, need for a fourth changed file, test import
failure, surviving targeted mutation, partial-cell policy ambiguity, or a hook refusal.
No skip, threshold relaxation, expected=3, or automatic rerun to turn red into green.

## Receipt
Write RUN/T05/last.md and closure.json (parent creates the approved directory).
Include exact paths/commands, changed file hashes, actual test counts, four-stage
mutation evidence, vendor gate, controls, and native_pty=NOT_RUN with reason
NOT_REQUIRED_FOR_THIS_MEMORY_INCREMENT. Report no commit created. If already fixed
by another completed worker, verify its evidence and report accepted-existing instead
of rewriting the solution.
